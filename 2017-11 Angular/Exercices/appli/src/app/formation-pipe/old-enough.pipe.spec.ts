import { OldEnoughPipe } from './old-enough.pipe';

describe('OldEnoughPipe', () => {
  it('create an instance', () => {
    const pipe = new OldEnoughPipe();
    expect(pipe).toBeTruthy();
  });
});
